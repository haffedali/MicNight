import React, {Component} from 'react';
import { Container } from '@material-ui/core';

const Container = (props) => {
    return <div><Container component={props} maxWidth={"sm"}/></div>
};

export default Container;